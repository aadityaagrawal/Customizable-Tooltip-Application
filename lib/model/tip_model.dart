import 'package:flutter/material.dart';

class TipModel extends StatelessWidget {
  const TipModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}